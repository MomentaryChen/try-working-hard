# try-working-hard

**語言：** [English](README.md) · 正體中文（本頁）

定時讓滑鼠做極小位移再移回，可用 GUI 設定間隔（分鐘）。僅供個人合法用途（例如避免簡報或閱讀時螢幕休眠），請遵守公司與服務條款。

## 環境需求

- **Windows**（使用 Win32 API 移動游標）
- **Python** 3.10 或以上
- 建議使用 **[uv](https://docs.astral.sh/uv/)** 管理環境與執行

## 安裝與執行

在專案根目錄：

```powershell
uv sync
uv run try-working-hard
```

或使用模組方式：

```powershell
uv run python -m mouse_jiggler
```

### Windows 單檔執行檔（不需安裝 Python）

在 **GitHub Releases** 可下載 **單一** `try-working-hard.exe`（PyInstaller 打包、無主控台視窗）。請到對應版本（例如 `v1.0.0`）的 **Assets** 下載後直接執行。

- 自行或 CI 如何建置：見 [docs/WINDOWS-BUILD.md](docs/WINDOWS-BUILD.md)（含 SmartScreen／防毒說明，以及 Briefcase 等替代方案簡介）。
- **發佈方式：** 推送 `v*` 標籤（例如 `v1.0.0`），[`.github/workflows/release.yml`](.github/workflows/release.yml) 會跑測試、建置並上傳至該 GitHub Release。

### 鍵盤與無障礙

- **F1** 開啟說明；**F2 / F3 / F4** 切換主要頁面；在**首頁 → 控制面板**時 **F5** 開始、**Shift+F5** 停止（條件允許時）；**Enter**／**Esc** 在**主視窗顯示中**時分別同開始／停止（僅在系統匣、主視窗未顯示時不作用）；**F6** 切換控制／紀錄。可**點擊「間隔」「位移」「持續微動」標籤**將焦點移到欄位。**Tab / Shift+Tab** 切換焦點。
- CustomTkinter 多數元件以**畫布**繪製，**螢幕閱讀器**支援度與完整原生 Win32 不同。詳見 [docs/ACCESSIBILITY.md](docs/ACCESSIBILITY.md)。

## 使用方式

1. 在左側欄使用 **繁中 / English** 分段切換介面語言。
2. 在「間隔（分鐘）」輸入數字（可為小數，例如 `0.5` 表示約 30 秒；最小 **0.1** 分鐘）。
3. 在「位移（像素）」輸入 **0–500** 的整數：游標會**水平**移動該距離後還原；**0** 表示該次不位移。預設 **100**。
4. 在「持續微動（秒）」輸入 **0–600**（可小數）：每次間隔到期後，會以短暫節奏**重複微動**這段時間，再進入下一輪等待；**0** 表示每輪只微動一次（與先前行為相同）。若位移為 **0**，此持續時間不生效。
5. 按 **開始** 啟動定時微動；**停止** 可結束。在左側欄選 **首頁**，再以 **控制面板 / 紀錄** 分段切換在控制與紀錄之間切換（英文模式下按鈕文字為英文）。
6. 執行中 **首頁頂端狀態區** 顯示 **倒數**（分:秒；滿一小時以上則為 時:分:秒），並以 **顏色提示**（等待下一輪微動為綠色調、**持續微動**階段為琥珀色調）。停止時為中性樣式。
7. **預設**：關閉視窗會 **停止排程並結束程式**。若勾選「關閉視窗時縮到系統匣」，關閉後會隱藏視窗並在通知區保留圖示，**排程會繼續**；在圖示上右鍵選單文字會依目前語言顯示。
8. 在 **設定** 頁可使用 **開啟設定檔**，以系統預設程式開啟 `config.json`（Windows 通常位於 `%APPDATA%\try-working-hard\`；未設定 `APPDATA` 時為 `~/.try-working-hard/`）。若檔案尚未存在，會先依目前設定寫入再開啟。

## 技術說明

- GUI：**CustomTkinter**（深色 / `dark-blue`、側欄切換主要頁面；**首頁**內以分段切換 **控制面板 / 紀錄**）
- 滑鼠：**ctypes** 呼叫 `user32.GetCursorPos` / `SetCursorPos`
- 系統匣：**pystray**、圖示繪製：**Pillow**（主視窗與匣區共用 PNG；重建 `.exe` 時另以 [`packaging/app.ico`](packaging/app.ico) 作為檔案圖示，見 [docs/WINDOWS-BUILD.md](docs/WINDOWS-BUILD.md)）

## 限制與注意

- 螢幕鎖定或部分遠端桌面環境下，行為可能與本機不同。
- 請勿用於規避應有的安全、監控或合規要求。

## 免責聲明

本軟體依「**現狀**」提供，**不提供任何明示或默示之擔保**（包括但不限於適銷性、特定用途之適用性、未侵權等）。您**自行決定是否使用**本軟體，並**自負全部風險**。

在任何情況下，作者或貢獻者對於因使用或無法使用本軟體所引起之**任何直接、間接、附帶、特別、衍生性或懲罰性損害**（包括但不限於資料遺失、業務中斷、設備損壞、違反公司規章或法令之後果），**概不負責**——即使已被告知該等損害之可能性。

您同意僅在**符合適用法律、雇主／學校政策、合約與服務條款**之前提下使用本軟體；**不得**將本軟體用於規避應有之資安控管、勞動或出勤監控、稽核、授權驗證，或其他違法或不當目的。因您使用本軟體所生之**一切法律與賠償責任**，由您自行承擔。

## 授權

本專案採用 **MIT License** 授權。完整條文見專案根目錄的 [`LICENSE`](LICENSE) 檔案。

簡要說明：在保留版權與許可聲明的前提下，您可自由使用、修改、散布與再授權本軟體；**軟體仍係依現狀提供、無擔保**（與上方免責聲明及 `LICENSE` 全文一致）。
