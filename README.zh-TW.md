# try-working-hard

![try-working-hard 介面示意](img/try-working-hard-v.1.2.0.gif)

**語言：** [English](README.md) · 正體中文（本頁）
Windows 桌面工具：只有在真實閒置達門檻時才微動游標。
模式：`路徑模式`（直線/圓形/方形）或 `自然模式`（不規則微移）。
每次微動後游標會回到原位置。
請僅用於合法且符合規範的個人情境。

**30 秒快速上手**
1. `uv sync`
2. `uv run try-working-hard`
3. 設定間隔與模式
4. 在首頁按 **開始**，確認倒數運作

**要看完整說明？** 請看下方 [使用方式](#使用方式) 與 [常見問題（快速版）](#常見問題快速版)。

## 目錄

- [環境需求](#環境需求)
- [安裝與執行](#安裝與執行)
- [使用方式](#使用方式)
- [常見問題（快速版）](#常見問題快速版)
- [技術說明](#技術說明)
- [限制與注意](#限制與注意)
- [免責聲明](#免責聲明)
- [授權](#授權)

## 環境需求

- **Windows**（使用 Win32 API 移動游標）
- **Python** 3.10 或以上
- 建議使用 **[uv](https://docs.astral.sh/uv/)** 管理環境與執行

## 安裝與執行

在專案根目錄：

```powershell
uv sync
uv run try-working-hard
```

或使用模組方式：

```powershell
uv run python -m mouse_jiggler
```

### Windows 單檔執行檔（不需安裝 Python）

在 **GitHub Releases** 會提供含版本號的單一執行檔：`try-working-hard-vX.Y.Z.exe`（例如 `try-working-hard-v1.3.0.exe`，PyInstaller 打包、無主控台視窗）。請到對應版本的 **Assets** 下載後直接執行。

- 自行或 CI 如何建置：見 [docs/WINDOWS-BUILD.md](docs/WINDOWS-BUILD.md)（含 SmartScreen／防毒說明，以及 Briefcase 等替代方案簡介）。
- **發佈方式：** 推送 `v*` 標籤（例如 `v1.0.0`），[`.github/workflows/release.yml`](.github/workflows/release.yml) 會跑測試、建置並上傳至該 GitHub Release。

### 鍵盤與無障礙

- **F1** 開啟說明；**F2 / F3 / F4** 切換主要頁面；在**首頁 → 控制面板**時 **F5** 開始、**Shift+F5** 停止（條件允許時）；**Enter**／**Esc** 在**主視窗顯示中**時分別同開始／停止（僅在系統匣、主視窗未顯示時不作用）；**F6** 切換控制／紀錄。可**點擊「間隔」「間隔隨機」「位移」「路徑速度」「活動模式」「移動路徑」標籤**將焦點移到欄位；間隔欄位下的 **30s / 1m / 5m / 10m** 為快速設定。**Tab / Shift+Tab** 切換焦點。
- CustomTkinter 多數元件以**畫布**繪製，**螢幕閱讀器**支援度與完整原生 Win32 不同。詳見 [docs/ACCESSIBILITY.md](docs/ACCESSIBILITY.md)。

## 使用方式

### 快速開始（建議）

1. 啟動程式後先在啟動提示選擇語言。
2. 設定 **間隔**（可直接按 `30s / 1m / 5m / 10m` 快速鍵）。
3. 選擇 **活動模式**（路徑／自然）並設定位移大小。
4. 到首頁控制面板按 **開始**。
5. 觀察首頁上方狀態列倒數，確認排程運作。

### 最常用設定

- **間隔：** 觸發一次微動前，需累積的閒置時間。
- **間隔隨機：** 讓每次間隔在 `間隔 ± N 秒` 內隨機化。
- **位移 / 路徑速度：** 控制移動距離與微動速度。
- **排程時段：** 可設定 `HH:MM` 開始/結束，也可在 `config.json` 用多段或 cron 進階規則。
- **系統匣行為：** 預設關閉視窗即停止並離開；可改成縮到系統匣持續執行。

### 快捷鍵與操作習慣

- **F1** 說明、**F2/F3/F4** 切換主要頁、**F6** 切換控制/紀錄。
- 在首頁控制面板：**F5** 開始、**Shift+F5** 停止。
- **Enter / Esc** 僅在主視窗可見時對應開始/停止。

### 詳細參考

- 一般啟動（非 `--start-in-tray`）會顯示啟動提示，可開啟流程簡介，且「不再顯示」會存到 `config.json` 的 `intro_acknowledged`。
- 設定頁可調整主題（`ui_theme`）、語言、自動檢查更新（`auto_check_updates`）、排程摘要與開啟 `config.json`。
- 分析頁提供 Matplotlib 圖表，統計資料存於與 `config.json` 同目錄的 `analytics.json`。
- 可用 `uv run python -m mouse_jiggler --start-in-tray`（或 `.exe` 同旗標）直接以僅系統匣啟動。

## 常見問題（快速版）

- **會一直移動滑鼠嗎？** 不會，必須先達到你設定的閒置時間。
- **游標會飄移嗎？** 不會，每次微動後會回到原位置。
- **不裝 Python 能用嗎？** 可以，下載 release 的 `.exe`。
- **可以直接縮在系統匣啟動嗎？** 可以，使用 `--start-in-tray`。
- **能用來規避公司規範嗎？** 不可以。

## 技術說明

- GUI：**CustomTkinter**—**淺色**改為更乾淨的 SaaS 色票（背景 **#F5F7FB**、側欄/卡片 **#FFFFFF**、邊框 **#E2E8F0**），主色互動為 **#3B82F6 / #2563EB / #1D4ED8**；**深色**改為現代深色系（背景 **#1E1E2E**、側欄 **#181825**、卡片 **#24273A**、邊框 **#313244**），主色互動為 **#89B4FA / #74C7EC / #B4BEFE**。**首頁**內以分段切換 **控制面板 / 紀錄**。
- **分析**：以 **Matplotlib**（TkAgg）嵌入；統計資料存於 **`analytics.json`**（與 **`config.json`** 同目錄）。
- 滑鼠：**ctypes** 呼叫 `user32.GetCursorPos` / `SetCursorPos`；自然模式可額外以 `mouse_event` 送左鍵與滾輪；排程以 `GetLastInputInfo` 與 `GetTickCount` 取得閒置時間
- 系統匣：**pystray**、圖示繪製：**Pillow**（主視窗與匣區共用 PNG；重建 `.exe` 時另以 [`packaging/app.ico`](packaging/app.ico) 作為檔案圖示，見 [docs/WINDOWS-BUILD.md](docs/WINDOWS-BUILD.md)）

## 限制與注意

- 螢幕鎖定或部分遠端桌面環境下，行為可能與本機不同。
- 請勿用於規避應有的安全、監控或合規要求。

## 免責聲明

本軟體依「**現狀**」提供，**不提供任何明示或默示之擔保**（包括但不限於適銷性、特定用途之適用性、未侵權等）。您**自行決定是否使用**本軟體，並**自負全部風險**。

在任何情況下，作者或貢獻者對於因使用或無法使用本軟體所引起之**任何直接、間接、附帶、特別、衍生性或懲罰性損害**（包括但不限於資料遺失、業務中斷、設備損壞、違反公司規章或法令之後果），**概不負責**——即使已被告知該等損害之可能性。

您同意僅在**符合適用法律、雇主／學校政策、合約與服務條款**之前提下使用本軟體；**不得**將本軟體用於規避應有之資安控管、勞動或出勤監控、稽核、授權驗證，或其他違法或不當目的。因您使用本軟體所生之**一切法律與賠償責任**，由您自行承擔。

## 授權

本專案採用 **MIT License** 授權。完整條文見專案根目錄的 [`LICENSE`](LICENSE) 檔案。

簡要說明：在保留版權與許可聲明的前提下，您可自由使用、修改、散布與再授權本軟體；**軟體仍係依現狀提供、無擔保**（與上方免責聲明及 `LICENSE` 全文一致）。
